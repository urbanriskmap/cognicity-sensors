-- # Sensors endpoint configuration

# AWS deployment
region: 'aws-region'
area_tag: 'country'

# VPC configuration
securityGroupIds: 'abc'
subnetIdA: '123'
subnetIdA: '456'

# Function configuration
timeout: 10

# Database parameters
PGHOST: 'localhost'
PGDATABASE: 'cognicity'
PGPASSWORD: 'p@ssw0rd'
PGPORT: 5432
PGSSL: 'true'
PGTIMEOUT: 10000
PGUSER: 'postgres'
